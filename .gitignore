node_modules/
# dist/ is committed on purpose: the Claude Code plugin runs it directly,
# so `/plugin install` must not require a build step.
*.tsbuildinfo
.DS_Store

# Written by `login`, and by the tests through PROMPT_STUDIO_HOME.
.prompt-studio/
