---
description: List your Prompt Studio projects
---

Call `list_projects` and show the result as a compact table: name, id, and when
it was last updated.

If it reports that they are not signed in, relay the sign-in command it gives
verbatim rather than paraphrasing it — the path differs per installation.
