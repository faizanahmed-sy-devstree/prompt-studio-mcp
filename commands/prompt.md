---
description: Get the build prompt the linked project generates
argument-hint: [web|mobile|backend]
---

Call `build_prompt` for the linked project, for surface $ARGUMENTS (ask which
if they did not say and the project has more than one).

Output the prompt in a single fenced block so it can be copied whole. Do not
summarise it, trim it, or comment inside the block — it is meant to be pasted
into another agent verbatim.
